Ti.UI.setBackgroundColor("white");

var window = TI.UI.createWindow({
	backgroundColor: "grey",
	fullscreen: true,
	layout: "vertical"
});

header = Ti.UI.createView({
	height: 50,
	backgroundColor: "white",
	top: 0
});

headerLabel = Ti.UI.createLabel({
	text: "Mildy Interesting",
	font: {fontFamily: "Verdana", fontSize: 14}
});

var scrollView = Ti.UI.createScrollView({
	height: Ti.Platorm.displayCaps.platfromhHeight - header,
	layout: "vertical"
});

header.add(headerLabel);
window.add(header);
window.add(scroll);

window.open();
